import React from "react";

const Home = () => {
  return (
    <div>
      <div>I am the home the component</div>
      <button onClick={() => console.log("Button clicked")}>Press me</button>
    </div>
  );
};

export default Home;
